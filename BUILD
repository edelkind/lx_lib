Run 'make' to build the library.

To build the speed tests, run 'make tests'.  They'll be located in ./bin.

To build programs associated with it, use:

	CFLAGS  = -I<lx_lib_dir>/lib
	LDFLAGS = -L<lx_lib_dir>/lib -llx
